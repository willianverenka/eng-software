[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lab07-servicos"
version = "0.1.0"
description = "Lab 07 MedSystem - servicos em Python"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["testes"]
python_files = ["test*.py"]
